import React from 'react';

const oo = () => {
    return (
        <div>
        <Link variant="contained" color="secondary" to={`/news/${id}`}>Show Details</Link>
        </div>
    );
};

export default oo;